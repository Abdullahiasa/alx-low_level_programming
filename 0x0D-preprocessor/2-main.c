#include <stdio.h>

/**
 * main -  prints the name of the file it was compiled from
 * followed by a newline
 * Return: zero success oherwise its a failure
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
Footer
