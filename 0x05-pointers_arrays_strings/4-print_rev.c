#include "main.h"
/**
 * print_rev - reverse the string.
 * @s: pointwe parameter.
 */

void print_rev(char *s)
{
	int i, j, k;
	
	for (i = 0; i < k; i++)
	{
		k--;
		j = s[i];
		s[i] = s[k];
		s[k] = j;
	}

}
